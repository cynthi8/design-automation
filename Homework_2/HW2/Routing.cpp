// Routing

#include "Placement.hpp"
#include "Graph.hpp"
#include "Routing.hpp"

void Routing::Route(Graph graph, Placement place)
{
	// What I need to get is a 2D vector of cells and null cells

	// Need to go thru and assign pins of dummy cells to cells that need it for feedthru

	// Then the actual cells can be split apart into 4 separate cells with each one having a terminal
	// There should be no rotating here

	// Then we can implement the algorithm for channel routing

	return;
}
